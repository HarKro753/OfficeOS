The demo will start with an already created app. It has a version hsitory of v1.0 being live. v1.0 has an upddate report.

We will create a update request for v1.1 with requesting a relatively simple history feature. the chat will have a hardcoded response firstly some questions then it will create the updated spec and the change request.

user will look at the source which redirects to a spec.md which shows like exactly how the task is thought off.

User will approve it.

After it has been approved user is redirected to dashboard where notiffciation is shown. The state is now request submitted its waiting for acceptance and then implementaiton. We want hardcoded waiting times for that. Finally when implemented it should show a message with something like "done" and the new version 1.1 should appear in history.

I click on v1.1 repot and the report should be really well it shows exactly the new screens and provides urls to the app store page.

The images are grouped into sections. Acceptance it must be clear exactly what has been done and the report should be really good.

It must only show the new image 
